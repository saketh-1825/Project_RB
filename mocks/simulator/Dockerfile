FROM alpine:latest

# A dummy simulator that keeps running to satisfy docker compose
# until the actual simulator code is added.
CMD ["sh", "-c", "echo 'Simulator mock starting...' && while true; do sleep 60; done"]
